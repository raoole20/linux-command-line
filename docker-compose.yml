services:
  linux-lab:
    environment:
      - TZ=America/caracas
    build: .
    container_name: linux-lab
    volumes:
      - .:/workspace
    stdin_open: true
    tty: true
    command: bash
    
